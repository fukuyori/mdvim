# mdvim

軽量・単一ファイルのVimキーバインド対応Markdownエディタ

**[オンラインで試す](https://fukuyori.github.io/mdvim/mdvim.html)** | **[English](./README.md)**

**バージョン 0.8.3**

## 特徴

- 🎯 **Vimキーバインド** - ノーマル、インサート、ビジュアルモード完全対応
- 📝 **ライブプレビュー** - リアルタイムMarkdownレンダリング、スクロール同期
- 📑 **目次（TOC）** - 見出しから自動生成、折りたたみ可能
- 🖼️ **画像対応** - ドラッグ＆ドロップ、クリップボードからペースト、エクスポートに埋め込み
- 📦 **単一HTMLファイル** - サーバー不要、ブラウザのみで動作
- 🌐 **多言語対応** - 日本語、英語、中国語、韓国語、ドイツ語
- 🎨 **テーマ** - ライト/ダークモード
- 💾 **自動保存** - セッション復旧機能、間隔設定可能

## クイックスタート

### 方法1: オンラインで使う（推奨）

ブラウザで直接開くだけ：

👉 **https://fukuyori.github.io/mdvim/mdvim.html**

インストール不要！

### 方法2: ダウンロードしてローカルで使う

1. [GitHubリポジトリ](https://github.com/fukuyori/mdvim)にアクセス
2. `mdvim.html`をダウンロード
3. ブラウザで開く
4. 編集開始！

### 方法3: ソースからビルド

```bash
git clone https://github.com/fukuyori/mdvim.git
cd mdvim/mdvim-source
npm install
node scripts/build-single.js
# dist/mdvim.html をブラウザで開く
```

## Vimコマンド

### モード

| キー | 動作 |
|-----|--------|
| `Esc` | ノーマルモードへ |
| `i` | カーソル前に挿入 |
| `a` | カーソル後に挿入 |
| `o` | 下に行を追加 |
| `O` | 上に行を追加 |
| `v` | ビジュアルモード |
| `V` | 行ビジュアルモード |

### 移動

| キー | 動作 |
|-----|--------|
| `h` `j` `k` `l` | 左、下、上、右 |
| `w` `b` | 単語の先頭へ（前方/後方） |
| `e` | 単語の末尾へ |
| `0` `$` | 行頭/行末 |
| `gg` `G` | 文書の先頭/末尾 |
| `H` `M` `L` | 画面の上/中央/下 |
| `Ctrl+f` `Ctrl+b` | ページダウン/アップ |
| `f{文字}` | 文字を前方検索 |
| `F{文字}` | 文字を後方検索 |
| `%` | 対応する括弧へ |

### 編集

| キー | 動作 |
|-----|--------|
| `x` | 文字削除 |
| `dd` | 行削除 |
| `dw` | 単語削除 |
| `D` | 行末まで削除 |
| `yy` | 行コピー |
| `p` `P` | 後/前にペースト |
| `u` | 元に戻す |
| `Ctrl+r` | やり直し |
| `.` | 直前の変更を繰り返し |
| `r{文字}` | 文字置換 |
| `~` | 大文字/小文字切り替え |
| `>>` `<<` | インデント/デデント |
| `J` | 行結合 |

### 検索

| キー | 動作 |
|-----|--------|
| `/パターン` | 前方検索 |
| `?パターン` | 後方検索 |
| `n` `N` | 次/前のマッチへ |
| `*` `#` | カーソル下の単語を検索 |

### Exコマンド

| コマンド | 動作 |
|---------|--------|
| `:w` | 保存 |
| `:e {ファイル}` | ファイルを開く |
| `:q` | 終了 |
| `:wq` | 保存して終了 |
| `:new` | 新規ファイル |
| `:export md` | Markdownとしてエクスポート |
| `:export html` | HTMLとしてエクスポート |
| `:import {url}` | URLからインポート |
| `:set wrap` | ワードラップ有効 |
| `:set nowrap` | ワードラップ無効 |
| `:noh` | 検索ハイライト解除 |
| `:help` | ヘルプ表示 |

## キーボードショートカット

| ショートカット | 動作 |
|----------|--------|
| `Ctrl+S` | 保存 |
| `Ctrl+O` | 開く |
| `Ctrl+N` | 新規ファイル |
| `Ctrl+P` | プレビュー切り替え |
| `Ctrl+Shift+T` | 目次切り替え |
| `Ctrl+Shift+V` | Vimモード切り替え |
| `F1` | ヘルプ |

## 画像の扱い

### 画像の追加

- **ドラッグ＆ドロップ**: 画像ファイルをエディタにドラッグ
- **ペースト**: `Ctrl+V`でクリップボードから貼り付け
- **Markdown記法**: `![代替テキスト](URL)`

### 画像付きエクスポート

- `:export md` - Markdownと画像を含む`.mdvim` ZIPファイルを出力
- `:export html` - 画像が埋め込まれたスタンドアロンHTMLを出力

## URLインポート

主要プラットフォームからMarkdownをインポート：

```vim
:import https://qiita.com/user/items/xxxxx
:import https://github.com/user/repo/blob/main/README.md
:import https://gist.github.com/user/xxxxx
:import https://example.com/document.md
```

## ワードラップ

ワードラップ有効時（`:set wrap`）：

| キー | 動作 |
|-----|--------|
| `j` `k` | 論理行で移動 |
| `gj` `gk` | 表示行で移動 |

## 設定

設定アイコン（⚙️）をクリックして設定：

- **テーマ**: ライト / ダーク
- **フォントサイズ**: 12-24px
- **自動保存間隔**: オフ / 5秒 / 10秒 / 30秒 / 60秒
- **言語**: 日本語, English, 中文, 한국어, Deutsch

## 対応ブラウザ

- Chrome/Edge 80以上
- Firefox 75以上
- Safari 13以上

## 開発

### プロジェクト構成

```
mdvim/
├── src/
│   ├── index.html          # HTMLテンプレート
│   ├── styles/main.css     # スタイル
│   └── ts/
│       ├── main.ts         # エントリポイント
│       ├── types.ts        # 型定義
│       ├── editor/         # Vimエディタコア
│       ├── parser/         # Markdownパーサー
│       ├── i18n/           # 翻訳
│       ├── storage/        # セッション管理
│       └── utils/          # ユーティリティ
├── scripts/
│   └── build-single.js     # ビルドスクリプト
├── dist/
│   └── mdvim.html          # ビルド済みファイル (135KB)
└── package.json
```

### ビルド

```bash
npm install
node scripts/build-single.js
```

### 依存関係

ランタイム（CDN経由で読み込み）:
- JSZip - ZIPファイル処理
- Turndown - HTML→Markdown変換

ビルド時:
- esbuild - TypeScriptバンドル

## ライセンス

MITライセンス - 詳細は[LICENSE](./LICENSE)を参照

## コントリビューション

貢献を歓迎します！お気軽にPull Requestを送ってください。
